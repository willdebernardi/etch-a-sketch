1. Remove outer method, and refactor by creating a new function that adds
    all of the neccessary event listeners in one fell swoop
2. Fix changeSize event listener by running new function on the newly created divs